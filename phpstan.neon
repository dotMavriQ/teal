includes:
    - vendor/larastan/larastan/extension.neon
    - phpstan-baseline.neon

parameters:
    level: max

    paths:
        - app

    # Keep analysis deterministic and fast
    parallel:
        maximumNumberOfProcesses: 4
